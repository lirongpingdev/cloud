spring:
  application:
    name: admin-client
  boot:
    admin:
      client:
        url: http://localhost:8006
server:
  port: 8007

management:
  endpoints:
    web:
      exposure:
        include: '*'
  endpoint:
    health:
      show-details: ALWAYS